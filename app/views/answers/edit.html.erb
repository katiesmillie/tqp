
<div class ="roboto">
    <div class="jumbotron">
      
     <h3> <%= @question.body %></h3>

      <%= form_tag answer_path(@answer.id), :method => "put" do %>
      <%= text_area_tag :body, @answer.body, :class => "input answerbox roboto" %>
      <p><a href="/answers"><%= submit_tag "Save", :class => "btn btn-large btn-success roboto" %></a></p>
      <%= hidden_field_tag :question_id, @question.id %>

      <% end %>
      <p class="roboto"><a href="/answers/new">show me a different question</a></p>
      
  </div>
    </div>

