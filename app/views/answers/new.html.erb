       <% yield %>
       <% #{insert the two snippets of javascript that you get from mixpanel with your specific key} %>
       <% record_mixpanel(flash[:mixpanel_record]) # this is a custom method we've defined in application_helper.rb %> 
  <div class ="roboto">
    
      <div class="jumbotron">
        
       <h3> <%= @question.body %></h3>

        <%= form_tag answers_path do %>
        <%= text_area_tag :body, nil, :class => "input answerbox roboto", :maxlength =>"255" %>
        <p><%= submit_tag "submit", :class => "btn btn-large btn-success roboto" %></p>
        <%= hidden_field_tag :question_id, @question.id %>
        <%= hidden_field_tag :round_id, @round.id %>
        <% end %>
  
        <p><%= pair_status(@round) %></p>
        

  
    </div>
  </div>

